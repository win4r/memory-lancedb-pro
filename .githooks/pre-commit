#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

if [[ "${SKIP_LOCAL_TESTS:-0}" == "1" ]]; then
  echo "[pre-commit] SKIP_LOCAL_TESTS=1 -> skipping local verification"
  exit 0
fi

./scripts/verify-before-submit.sh
